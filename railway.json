{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash build.sh",
    "nixpacksPlan": {
      "providers": [
        "python",
        "node"
      ],
      "phases": {
        "setup": {
          "nixPkgs": ["python3", "nodejs_22"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --pythonpath ."
  }
}
