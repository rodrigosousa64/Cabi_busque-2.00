{
  "name": "cabi_busque",
  "version": "1.0.0",
  "description": "Root package.json to run both frontend and backend concurrently.",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && ..\\venv\\Scripts\\python manage.py runserver",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
